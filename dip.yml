version: '2'

compose:
  files:
    - docker-compose.yml

interaction:
  bash: &bash
    service: backend
    command: bash

  yarn:
    <<: *bash
    command: yarn
    subcommands:
      start:
        compose_method: up

provision:
  - dip yarn install
